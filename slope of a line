// This program calculates the slope of a straight line by populating the coordinates of two points in the form of (x,y), and finds the distance between them.

class Main {
  public static void main(String[] args) {
  
  // declaring variables
  double x1 = 4;
  double x2 = 5;
  double y1 = 10;
  double y2 = 7;

  // calculations for slope 
  double slope = (y2-y1/x2-x1);

  // calculations for distance
 double distance =  Math.sqrt((y2 - y1) * (y2 - y1) + (x2 - x1) * (x2 - x1));

  System.out.println("The slope is " + slope + " and the distance between the two points is  " + distance);
 
}


    
}
